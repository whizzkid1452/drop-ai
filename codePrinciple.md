# Code Principles

이 프로젝트는 다음의 핵심 개발 원칙을 준수하여 작성됩니다. 우리는 단순히 작동하는 코드를 넘어, **"수정하기 쉬운 코드"**를 지향합니다.

---

## 1. 가독성 (Readability)

가독성은 코드가 읽기 쉬운 정도를 말합니다. 코드가 변경하기 쉬우려면 먼저 코드가 어떤 동작을 하는지 이해할 수 있어야 합니다. 읽기 좋은 코드는 읽는 사람이 한 번에 머릿속에서 고려하는 맥락이 적고, 위에서 아래로 자연스럽게 이어집니다.

- **이름 붙이기 (Naming)**
  - **목적물과 행위가 확실하게 드러나는 변수명 사용**: 변수명만으로 역할과 생명주기를 파악할 수 있어야 하며, 이는 주석을 대신합니다.
  - **모호한 이름 지양**: `data`, `item`, `flag`와 같은 일반적인 명사나 축약어 대신 구체적인 이름을 사용합니다.
    - **Good:** `userProfileImage`, `fetchTrackList`, `isRegionSelected`
    - **Bad:** `img`, `list`, `temp`
  - 복잡한 조건이나 매직 넘버에 의미 있는 이름을 붙입니다.
- **맥락 줄이기**
  - 같이 실행되지 않는 코드 분리하기
  - 구현 상세 추상화하기
  - 로직 종류에 따라 합쳐진 함수 쪼개기
- **위에서 아래로 읽히게 하기**
  - **주석 대신 코드**: "이 코드는 ~을 합니다"라는 주석 대신, 그 내용을 담은 함수명으로 추출합니다.
  - **Why 위주의 주석**: 주석은 '무엇(What)'이 아닌 '왜(Why)'를 설명할 때만 제한적으로 사용합니다.
  - 시점 이동(Jump) 줄이기 및 삼항 연산자 단순하게 유지하기

## 2. 예측 가능성 (Predictability)

함수나 컴포넌트의 동작을 동료들이 얼마나 예측할 수 있는지를 말합니다. 일관적인 규칙을 따르고, 이름과 파라미터, 반환 값만 보고도 동작을 짐작할 수 있어야 합니다.

- **이름 관리**: 이름이 겹치지 않게 관리하여 혼선을 방지합니다.
- **타입 통일**: 같은 종류의 함수는 반환 타입을 통일합니다.
- **숨은 로직 드러내기**: 부수 효과(Side Effect)를 최소화하고, 중요한 로직은 겉으로 드러냅니다.

## 3. 응집도 (Cohesion)

수정되어야 할 코드가 항상 같이 수정되는지를 말합니다. 응집도가 높은 코드는 코드의 한 부분을 수정해도 의도치 않게 다른 부분에서 오류가 발생하지 않도록 구조적으로 뒷받침됩니다.

- **관심사 분리 (Separation of Concerns)**: 비즈니스 로직, 데이터 처리, UI 렌더링을 명확히 분리합니다.
- **물리적 응집**: 함께 수정되는 파일(스타일, 로직, 뷰)은 같은 디렉토리에 두어 유지보수성을 높입니다.
- **공통화**: 함께 수정되지 않으면 오류가 발생하는 경우, 적극적으로 공통화/추상화하세요.
- **전략적 중복**: 위험성이 높지 않다면 가독성을 위해 코드 중복을 허용할 수 있습니다.

## 4. 결합도 (Coupling)

코드를 수정했을 때의 영향 범위를 말합니다. 영향 범위가 적고 예측 가능한 코드가 수정하기 쉬운 코드입니다.

- **책임 분산**: 하나의 모듈이 너무 많은 책임을 지지 않도록 관리합니다.
- **의존성 관리 (Dependency Management)**: 직접 의존을 피하고 추상화와 의존성 주입(DI)을 지향합니다.
  - 상위 모듈이 하위 모듈의 구체적인 구현에 의존하지 않도록 합니다.
  - 결합도를 낮추기 위해 인터페이스나 훅(Hook)을 통한 주입 패턴을 활용합니다.
- **Props Drilling 방지**: 불필요한 데이터 전달 경로를 제거하고 관심사를 격리합니다.

---

## 코드 품질의 균형 (Balance & Trade-off)

아쉽게도 위 4가지 기준을 항상 동시에 충족하기는 어렵습니다. 프론트엔드 개발자는 상황에 따라 최선의 가치를 선택해야 합니다.

- **응집도 vs 가독성**: 공통화/추상화를 하면 응집도는 높아지지만, 코드가 한 차례 추상화되기 때문에 가독성이 떨어질 수 있습니다.
- **결합도 vs 응집도**: 중복 코드를 허용하면 영향 범위를 줄여 결합도는 낮아지지만, 한쪽을 수정했을 때 다른 쪽을 누락할 위험(응집도 저하)이 생깁니다.

> [!IMPORTANT]
> 장기적으로 코드가 수정하기 쉽게 하기 위해, 현재 직면한 문제의 성격에 따라 어떤 가치를 우선할지 깊이 있게 고민하고 팀과 공유합니다.
