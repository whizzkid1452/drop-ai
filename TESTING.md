# AI 에이전트 테스트 가이드

## 🚀 빠른 시작

### 1. 환경 변수 설정

`.env` 파일을 열어서 OpenAI API 키를 입력하세요:

```bash
OPENAI_API_KEY=sk-your-actual-api-key-here
OPENAI_MODEL=gpt-4o
PORT=3001
```

### 2. 서버 실행

**옵션 1: 동시 실행 (권장)**
```bash
pnpm dev:all
```

**옵션 2: 별도 터미널에서 실행**
```bash
# 터미널 1: Express 서버
pnpm dev:server

# 터미널 2: Vite 개발 서버
pnpm dev
```

### 3. 브라우저에서 확인

1. `http://localhost:5173` 접속
2. `/daw` 경로로 이동
3. 오른쪽에 AI 채팅 패널이 표시됩니다

## 🧪 테스트 시나리오

### 기본 테스트

1. **트랙이 없는 경우:**
   - "안녕하세요"라고 입력
   - AI가 인사하고 도움을 제안합니다

2. **트랙이 있는 경우:**
   - 먼저 오디오 파일을 업로드하여 트랙을 생성
   - "볼륨을 키워줘" 또는 "볼륨을 -6dB로 설정해줘" 입력
   - AI가 `set_volume` 도구를 호출하고 실제로 볼륨이 변경됩니다

### 도구별 테스트 명령어

| 도구 | 테스트 명령어 예시 |
|------|-------------------|
| `set_volume` | "볼륨을 키워줘", "소리를 작게 해줘", "볼륨을 -10dB로 설정해줘" |
| `trim_audio` | "3초부터 10초까지만 남겨줘", "앞부분 5초를 잘라줘" |
| `apply_reverb` | "리버브 추가해줘", "공간감을 줘", "잔향을 길게 해줘" |
| `apply_filter` | "소리를 밝게 해줘", "저음을 제거해줘" |
| `apply_eq` | "저음을 강조해줘", "고음을 부스팅해줘" |
| `fade_in` | "페이드 인 추가해줘" |
| `fade_out` | "페이드 아웃 추가해줘" |
| `mute_track` | "이 트랙 음소거해줘" |
| `solo_track` | "이 트랙만 들려줘" |
| `get_track_info` | "트랙 정보 알려줘" |

## 🔍 디버깅

### 서버 로그 확인

Express 서버 터미널에서 다음을 확인하세요:
- API 요청이 들어오는지
- OpenAI API 호출이 성공하는지
- 에러 메시지가 있는지

### 브라우저 콘솔 확인

개발자 도구(F12)에서:
- 네트워크 탭: `/api/chat` 요청 확인
- 콘솔 탭: 에러 메시지 확인

### 일반적인 문제

1. **"OpenAI API 키가 유효하지 않습니다"**
   - `.env` 파일의 `OPENAI_API_KEY` 확인
   - 서버를 재시작

2. **"CORS 오류"**
   - Vite 프록시 설정 확인 (`vite.config.ts`)
   - Express 서버가 3001 포트에서 실행 중인지 확인

3. **"트랙을 찾을 수 없습니다"**
   - 먼저 오디오 파일을 업로드하여 트랙을 생성
   - 트랙 ID가 올바른지 확인

## 📊 성공적인 테스트 확인 사항

✅ AI가 사용자 메시지를 이해하고 적절한 도구를 선택
✅ 도구 호출이 성공적으로 실행됨
✅ Zustand 스토어의 트랙 상태가 실제로 변경됨
✅ UI에 변경사항이 반영됨
✅ 채팅에 실행 결과가 표시됨

## 🎯 다음 단계

테스트가 성공하면:
1. Tone.js 통합 - 실제 오디오 처리 구현
2. 오디오 내보내기 기능
3. 더 복잡한 편집 시나리오 테스트

